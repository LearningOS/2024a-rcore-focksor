# 实验报告

## 实现功能
在本章节实验中，我实现了：
1. 重写 sys_get_time 和 sys_task_info。实现原理主要是先在内核空间获取相应的信息之后，将其写入到用户空间相应的内存中。
2. mmap 和 munmap 匿名映射。其实现方式是在进行 mmap, munmap 操作时，在物理内存分配相应的页，并在该进程对应的用户地址空间增加相应的记录。

## 简答作业
### 1. 请列举 SV39 页表页表项的组成，描述其中的标志位有何作用？
SV39页表的页表项组成：
* V: 是否有效
* R: 是否可读
* W: 是否可写
* X: 是否可执行
* U: 用户态是否可访问
* G: 是否为全局项
* A: 是否被访问过
* D: 是否为脏页
* RSW: S态软件保留位
* PPN[0], PPN[1], PPN[2]: 物理页号
> 表项标志位表述参照《riscv-privileged-v1.10》第63页

### 2. 缺页
> 缺页指的是进程访问页面时页面不在页表中或在页表中无效的现象，此时 MMU 将会返回一个中断， 告知 os 进程内存访问出了问题。os 选择填补页表并重新执行异常指令或者杀死进程。

#### 请问哪些异常可能是缺页导致的？
从[scause.rs](.cargo/git/checkouts/riscv-ab2abd16c438337b/11d43cf/src/register/scause.rs)L36中列出的异常情况来看，可能由缺页导致的异常有：
* LoadFault：CPU尝试从内存加载数据时可能会缺页
* InstructionPageFault：指令未映射到物理页
* LoadPageFault：CPU尝试从内存加载数据时可能会缺页
* StorePageFault：CPU尝试存储数据时可能会缺页
* InstructionGuestPageFault, LoadGuestPageFault, StoreGuestPageFault：应该是跟上面几项一样的，但是目前暂未理解 Guest 的作用和相关机制。

#### 发生缺页时，描述相关重要寄存器的值，上次实验描述过的可以简略。
上次实验中描述了部分异常相关的寄存器，缺页也是一种异常。在发生缺页时，除了常规记录异常原因、异常现场相关的寄存器外，还有一个重要的寄存器是 `satp`。

该寄存器存储了当前活动页表的基地址，当发生缺页异常时，系统可以通过该地址找到对应的内存表项，并根据需要尝试为其分配新页。


----

缺页有两个常见的原因，其一是 Lazy 策略，也就是直到内存页面被访问才实际进行页表操作。 比如，一个程序被执行时，进程的代码段理论上需要从磁盘加载到内存但是 os并不会马上这样做， 而是会保存 .text 段在磁盘的位置信息，在这些代码第一次被执行时才完成从磁盘的加载操作。

#### 这样做有哪些好处？
一个程序并不是所有代码都会被执行到的，特别是对于一个比较大的程序，使用 lazy 策略在实际执行代码时再将其加载到内存中，可以减少内存使用量。

----
其实，我们的 mmap 也可以采取 Lazy 策略，比如：一个用户进程先后申请了 10G 的内存空间， 然后用了其中 1M 就直接退出了。按照现在的做法，我们显然亏大了，进行了很多没有意义的页表操作。

#### 处理 10G 连续的内存页面，对应的 SV39 页表大致占用多少内存 (估算数量级即可)？
已知每个页大小为 4K，则 10G 的内存至少需要 10G/4K = 10 * 2^18 个页表项，每个页表项为 8 字节，因此大约需要 10 * 2^18 * 8 = 20M内存。

#### 请简单思考如何才能实现 Lazy 策略，缺页时又如何处理？描述合理即可，不需要考虑实现。
可以在mmap的时候仅记录虚拟地址信息，而不实际从物理内存中分配，在实际使用内存时，系统会触发缺页异常，此时我们查找是否有相关的记录，并补充未映射的物理内存映射。

----
缺页的另一个常见原因是 swap 策略，也就是内存页面可能被换到磁盘上了，导致对应页面失效。
#### 此时页面失效如何表现在页表项(PTE)上？
当页面失效时，Valid标志应被置为0。

### 3. 双页表与单页表
> 为了防范侧信道攻击，我们的 os 使用了双页表。但是传统的设计一直是单页表的，也就是说，用户线程和对应的内核线程共用同一张页表，只不过内核对应的地址只允许在内核态访问。 (备注：这里的单/双的说法仅为自创的通俗说法，并无这个名词概念，详情见 KPTI )
#### 在单页表情况下，如何更换页表？
1. 切换到内核态
2. 保存当前的satp
3. 切换satp到新页表基址
4. 恢复到用户态

#### 单页表情况下，如何控制用户态无法访问内核页面？（tips:看看上一题最后一问）
可以设置U标志位，当为0时，用户态无法访问。

#### 单页表有何优势？（回答合理即可）
1. 用户空间和内核空间共用一张表，结构比较简单
2. 节约内存

#### 双页表实现下，何时需要更换页表？假设你写一个单页表操作系统，你会选择何时更换页表（回答合理即可）？
双页表在以下场景中需要更换页表：
1. 切换进程
2. 内核态、用户态之间切换
单页表操作系统更换页表：
1. 切换进程
2. 内核态、用户态之间切换


## 荣誉准则

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

    > 未有交流的对象

2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：
   * riscv-privileged-v1.10

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

## 你对本次实验设计及难度/工作量的看法
最后一道简单题中涉及了单页表相关，但是课程中实际没有体现这部分的内容，会有些莫名其妙，也无法根据所学内容做出合适的回答。
